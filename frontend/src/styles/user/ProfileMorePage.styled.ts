import styled from 'styled-components'
import { breakpoints } from '../../constants/Styles'

export const ProfileMoreContainer = styled.div`
  padding: 5%;

  ${breakpoints.large} {
    padding: 2%;
  }
`

export const ProfileMoreTitle = styled.h2`
  margin-bottom: 10px;
`
